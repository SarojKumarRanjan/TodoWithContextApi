import React from "react"



const UserContext = React.createContext()

export default UserContext;

/* we use userContextg as a wrapper and its called provider it provide variable access to its inner elements 
we use it as <userContext>   </userContext> */